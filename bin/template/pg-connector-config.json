{
  "name": "debezium-connector",  
  "config": {
    "connector.class": "io.debezium.connector.postgresql.PostgresConnector", 
    "database.hostname": "localhost", 
    "database.port": "5432", 
    "database.user": "pg", 
    "database.password": "pg", 
    "database.dbname" : "pg", 
    "database.server.name": "oltp-olap-sync",
    "table.include.list": "public.student,public.contact"
  }
}